[project]
name = "home-assistant-pv-forecast-fusion"
version = "0.1.0"
description = "Home Assistant custom integration for explainable PV forecast fusion"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
